# Retained source bytes are hashed and bound into every snapshot. Any line-ending
# translation on checkout would change those bytes and break source_binding.sha256,
# so nothing under artifacts/ is treated as text.
artifacts/** -text
